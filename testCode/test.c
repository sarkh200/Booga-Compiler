#include <stdio.h>
int main(void)
{
	char m[30000] = {0};
	int i = 0;
	while (m[i] != 0)
	{
		putchar(m[i]);
		while (m[i] != 0)
		{
			putchar(m[i]);
		}
		m[i] = getchar();
		putchar(m[i]);
		putchar(m[i]);
		m[i] = getchar();
		m[i] = getchar();
		m[i] = getchar();
		m[i]++;
		m[i] = getchar();
		m[i]--;
		m[i] = getchar();
		i--;
		i++;
		m[i] = getchar();
		while (m[i] != 0)
		{
		}
		putchar(m[i]);
		putchar(m[i]);
	}
	m[i]++;
	m[i]++;
	m[i]++;
	m[i]++;
	m[i]++;
	m[i]++;
	m[i]++;
	m[i]++;
	while (m[i] != 0)
	{
		i++;
		m[i]++;
		m[i]++;
		m[i]++;
		m[i]++;
		while (m[i] != 0)
		{
			i++;
			m[i]++;
			m[i]++;
			i++;
			m[i]++;
			m[i]++;
			m[i]++;
			i++;
			m[i]++;
			m[i]++;
			m[i]++;
			i++;
			m[i]++;
			i--;
			i--;
			i--;
			i--;
			m[i]--;
		}
		i++;
		m[i]++;
		i++;
		m[i]++;
		i++;
		m[i]--;
		i++;
		i++;
		m[i]++;
		while (m[i] != 0)
		{
			i--;
		}
		i--;
		m[i]--;
	}
	i++;
	i++;
	putchar(m[i]);
	i++;
	m[i]--;
	m[i]--;
	m[i]--;
	putchar(m[i]);
	m[i]++;
	m[i]++;
	m[i]++;
	m[i]++;
	m[i]++;
	m[i]++;
	m[i]++;
	putchar(m[i]);
	putchar(m[i]);
	m[i]++;
	m[i]++;
	m[i]++;
	putchar(m[i]);
	i++;
	i++;
	putchar(m[i]);
	i--;
	m[i]--;
	putchar(m[i]);
	i--;
	putchar(m[i]);
	m[i]++;
	m[i]++;
	m[i]++;
	putchar(m[i]);
	m[i]--;
	m[i]--;
	m[i]--;
	m[i]--;
	m[i]--;
	m[i]--;
	putchar(m[i]);
	m[i]--;
	m[i]--;
	m[i]--;
	m[i]--;
	m[i]--;
	m[i]--;
	m[i]--;
	m[i]--;
	putchar(m[i]);
	i++;
	i++;
	m[i]++;
	putchar(m[i]);
	i++;
	m[i]++;
	m[i]++;
	putchar(m[i]);
}